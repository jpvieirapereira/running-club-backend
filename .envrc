# DynamoDB
export DYNAMODB_USERS_TABLE=users

# S3
export S3_BUCKET_NAME=servidor-files

# API
export API_V1_PREFIX=/api/v1

# Application
export APP_NAME=servidor
export ENVIRONMENT=local
export DEBUG=true
export LOG_LEVEL=INFO

# AWS Configuration
export AWS_REGION=us-east-1

dotenv_if_exists

export AWS_ENDPOINT_URL=http://localhost:4566
export DYNAMO_ENDPOINT=http://localhost:4566
export AWS_ENDPOINT_URL_DYNAMODB=http://localhost:4566
